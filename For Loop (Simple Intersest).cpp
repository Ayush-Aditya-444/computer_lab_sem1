#include<stdio.h>
int main()
{
	int p, n, i;
	float r, si;
	
	for (i=1; i<=1; i++)
	{
		printf("Enter values of p, n, r");
		scanf("%d %d %f", &p, &n, &r);
		si=(p*n*r)/100;
		printf("%f", si);
		return 0;
		
	 } 
}
